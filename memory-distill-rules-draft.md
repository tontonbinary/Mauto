# L1 蒸馏规则草案（2026-05-03 确认版）

---

## 一、入口机制

**SelfEvolve 是所有纠正和知识的入口。**

任何来自用户的纠正、反馈、知识输入，先进入 SelfEvolve，再根据规则分流到 RuleDecision 或 SocialEcology。

---

## 二、纠正的分流规则

**7 天计数制**：以最近 7 天（从今天往前推 7 天）的 L1 日志中该纠正的记录次数为准。

| 纠正次数 | SelfEvolve 记录 | RuleDecision 记录 |
|---------|-----------------|-------------------|
| 第 1 次 | 完整内容记录 | 无 |
| 第 2 次 | 摘要 + 次数标记（"纠正×2，已升级规则"） | 完整内容 |
| 第 3 次及以上 | 更新次数标记（"纠正×3"） | 已存在，不重复 |

**格式示例**：
```
SelfEvolve
用户反馈：先给方案再执行（纠正×2，已升级规则）

RuleDecision
做任何代码或配置修改，先给方案，提示风险，确认后执行
```

**重置规则**：如果 7 天内没有再次被纠正，该主题的纠正计数重置。

---

## 三、知识的分流规则

所有知识先进入 SelfEvolve，再根据是否与系统/工具/组织相关分流：

| 知识类型 | 归属 | 判断标准 |
|---------|------|---------|
| 属于系统/工具/组织 | SocialEcology | 跟我所在的系统、工具、组织、渠道、协作网络有关 |
| 属于个人技能/通用知识 | SelfEvolve（保留） | 跟我个人的工作技能、通用常识、领域知识有关 |

**正面案例**：
> "OpenClaw 支持多 Agent 管理" → **SocialEcology**（系统功能，跟我环境有关）
> "飞书支持消息撤回" → **SocialEcology**（渠道工具功能）
> "Mautoer 负责系统维护" → **SocialEcology**（组织角色）
> "水到 100 度变蒸汽" → **SelfEvolve**（通用知识，跟环境无关）
> "Python 3.12 引入新语法" → **SelfEvolve**（个人技能知识）

**反面案例**：
> "Python 很好学" → 主观评价，不记
> "你应该用 Python" → 这是建议/纠正，进 SelfEvolve

---

## 四、只记一次原则（调整后）

**完整内容只在一个分类中出现：**

| 场景 | 完整内容位置 | 其他分类 |
|------|-------------|---------|
| 纠正 1 次 | SelfEvolve（完整） | RuleDecision 无 |
| 纠正 2 次 | RuleDecision（完整） | SelfEvolve 只留摘要+次数标记 |
| 知识属于环境 | SocialEcology（完整） | SelfEvolve 无 |
| 知识属于个人 | SelfEvolve（完整） | SocialEcology 无 |
| 待办/产出 | To-do/Output（完整） | 其他分类无 |

**禁止**：同一件事的完整内容同时出现在两个分类中。

---

## 五、6 分类详细解释 + 案例

### RuleDecision（硬性规则）

**定义**：通过 2 次以上纠正升级而来的，或用户一次性建立的硬性做事规范。

**判断标准**：
- 纠正次数 ≥ 2（以近 7 天 L1 记录为准）
- 用户明确说"必须"、"以后都"、"禁止"、"先...再..."
- 从 SelfEvolve 升级而来

**正面案例**：
> "做任何代码或配置修改，先给方案，提示风险，确认后执行"（纠正 2 次后升级）→ **RuleDecision**

**反面案例**：
> "我觉得这个方案可以" → 这是决策，不是规则，进 Event
> "我习惯用暗色主题" → 这是偏好，进 SelfEvolve

---

### SelfEvolve（自我进化）

**定义**：所有纠正和知识的入口。记录用户反馈、个人教训、通用知识、偏好习惯。

**作为入口时的记录**：
- 纠正 1 次：完整记录纠正内容和正确做法
- 纠正 2 次：改为摘要+次数，完整内容移至 RuleDecision
- 知识（个人/通用）：完整保留

**正面案例（纠正入口）**：
> "用普通对话的方式发给我，不要用代码块"（第 1 次）→ **SelfEvolve**
> "先给方案再执行"（第 2 次，7 天内第 2 次）→ **SelfEvolve** 改标记，**RuleDecision** 写规则

**正面案例（知识入口）**：
> "Python 3.12 引入新语法特性" → **SelfEvolve**（个人技能知识）

**反面案例**：
> "这个方案有问题"但没说正确做法 → 无结论，进 Event
> "今天天气不错" → 闲聊，不记
> "OpenClaw 支持多 Agent" → 进 SocialEcology（环境知识）

---

### SocialEcology（环境认知）

**定义**：来自 SelfEvolve 的知识入口分流。记录客观环境信息：角色定位、协作关系、渠道工具、基础设施。

**判断标准**：
- "负责..."、"管理..."、"对接..."（角色分工）
- "用飞书/微信/邮件..."（沟通渠道）
- "系统部署在..."、"数据库是..."（基础设施）
- "跟...协作"、"需要...提供"（协作关系）
- 来自 SelfEvolve 的"环境类知识"

**vs 其他分类的边界**：
- vs RuleDecision：SocialEcology 是"是什么"（客观结构），RuleDecision 是"该怎么做"（行为规范）
- vs SelfEvolve：SocialEcology 是"客观环境信息"，SelfEvolve 是"主观偏好/教训/个人知识"
- vs Event：SocialEcology 是"长期有效的结构信息"，Event 是"一次性发生的事"

**正面案例**：
> "Mautoer 负责系统维护" → **SocialEcology**（角色定位）
> "Xiaoxian 负责资料收集" → **SocialEcology**（协作关系）
> "TS 负责商业服务" → **SocialEcology**（协作关系）
> "沟通渠道是飞书" → **SocialEcology**（渠道工具）
> "OpenClaw 支持多 Agent 管理" → **SocialEcology**（系统功能）

**反面案例**：
> "以后用飞书发消息" → 这是规则（"以后都"），进 RuleDecision
> "飞书不好用" → 这是主观评价，进 SelfEvolve（偏好）
> "今天飞书崩了" → 这是事件，进 Event

---

### To-do（待办）

**定义**：用户或 Agent 承诺要做但还没做的事。包括搁置、延后、备忘。

**判断标准**：
- "明天再弄"、"先放一放"、"记得改"、"以后再说"
- 用户要求 Agent"记着这个"
- Agent 承诺"我稍后处理"

**正面案例**：
> "群聊 session 不被蒸馏的问题先搁置" → **To-do**
> "Fix 4 记入多维表，搁置状态" → **To-do**

**反面案例**：
> "已经完成" → 不是待办，进 Output
> "我觉得应该做这个" → 没承诺，进 Event

---

### Output（产出）

**定义**：已完成、有明确结果的事项。含文件路径、交付物、完成的改动。

**判断标准**：
- "写完了"、"提交了"、"已创建"、"已实现"
- 有文件路径、URL、文档链接
- 用户确认"搞定"、"可以了"

**正面案例**：
> "HEARTBEAT.md 已更新" → **Output**
> "创建了 memory_distill_reminder.py" → **Output**

**反面案例**：
> "正在写" → 未完成，不记或进 Event
> "计划做这个" → 未开始，进 To-do

---

### Event（兜底事件）

**定义**：不属于以上 5 类的所有其他内容。客观事实、讨论过程、信息获取。

**判断标准**：
- 排除了 RuleDecision/SelfEvolve/SocialEcology/To-do/Output 特征后剩下的
- 无明确规则/偏好/环境认知/待办/产出属性

**正面案例**：
> "用户要求整理 prompt 内容" → **Event**
> "讨论了 MiniMax API 的配置" → **Event**
> "发现群聊 session 不被蒸馏的 bug" → **Event**（发现事实，无规则/待办/产出）

**反面案例**：
> "用户要求整理 prompt"但用户同时说"你整理完发给我" → 这包含 To-do 属性，优先进 To-do

---

## 六、多日重复处理规则

**原则**：新一天的 L1 里，对持续进行中的事项，用**引用方式简短记录**，不重复全文。

**格式**：
```
Event
[4/27-持续] Mauto简化：移除L3、改Agent主动总结
      5/3：重写蒸馏规则 — 待确认
```

**判断标准**：
- 同一个主题词（如"Mauto简化"）在近 3 天 L1 里已出现
- 今天有新进展 → 新 L1 里写"自X月X日起：摘要（持续）+ 今日进展"
- 今天无新进展 → 不写

---

## 七、内容写法标准

1. **一句话精华**：禁止日记体，禁止逐条复述，禁止超过 100 字
2. **标签 = 核心主题**：用具体主题标签，不泛泛写"讨论"、"聊天"
3. **必须带 source**：标明消息来源（session 日期或消息序号），方便回溯
4. **情绪必录**：用户表达积极/负面情绪时，必须记录 Emotion 字段

---

## 八、写入前自检清单

Agent 每次写 L1 前必须过一遍：

- [ ] SelfEvolve 入口检查：用户的纠正/知识/反馈，正确分流了吗？
- [ ] 纠正次数检查：近 7 天这个主题被纠正了几次？够 2 次要升 RuleDecision 吗？
- [ ] RuleDecision 有没有漏？（已升级的纠正、硬性规则）
- [ ] SocialEcology 有没有漏？（角色/渠道/协作关系/系统知识）
- [ ] To-do 有没有漏？（搁置、承诺、备忘）
- [ ] Output 有没有漏？（已完成的事项、文件、交付物）
- [ ] Event 兜底：剩下的内容合理吗？
- [ ] 近 3 天 L1 有没有重复内容？（有则引用，不写全文）
- [ ] 内容是否一句话精华？（超过 100 字要压缩）

---

## 九、改动范围 + 风险

| 文件 | 改动内容 | 风险 |
|------|---------|------|
| `memory/l1_writer.py` | CATEGORIES 从7个改6个；支持"纠正次数"标记和分流逻辑 | 现有L1文件格式会变，但 l1_writer 是覆盖写，**历史文件不影响**，新格式从下次写入生效 |
| `memory/memory-rules.md` | 重写分类说明和入口机制 | 无风险，纯文档 |
| `AGENTS.md` | 更新记忆管理规范 | 无风险 |
| `HEARTBEAT.md` | 替换 reminder 脚本 | 无风险 |
| `scripts/memory_distill_reminder.py` | 重写 | 无风险 |

**唯一风险**：如果其他 Agent（TS、Xiaoxian）也共享这套 l1_writer，它们的 L1 格式也会变。需要确认是否共用，或做 agent 级配置隔离。

---

*状态：讨论确认版，等待最终执行*
